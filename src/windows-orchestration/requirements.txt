# Requirements for Windows Orchestration Service
# Install with: pip install -r requirements.txt
Flask==3.1.3
Flask-CORS==6.0.2
requests==2.33.1
python-dotenv==1.2.2
# STT: faster-whisper (CTranslate2) — Foundry Local has no Whisper REST endpoint
faster-whisper
# TTS: kokoro-onnx (neural, offline, win-arm64 native via onnxruntime)
kokoro-onnx
onnxruntime
soundfile
# TTS fallback: pyttsx3 SAPI5 (robotic but guaranteed on all Windows)
pyttsx3
# Misty controller: WebSocket client for event subscriptions
websocket-client
# Wake word detection: laptop mic + openWakeWord (issue #44)
sounddevice
openwakeword
# BLE provisioning: WiFi setup over Bluetooth (issue #111)
bleak
