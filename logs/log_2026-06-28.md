# Log — 2026-06-28

## Requests

1. Implement issue #70: Centralize shared configuration defaults to prevent drift.
   - Create canonical source of truth for all env-configurable defaults
   - Fix documented values that had drifted from actual code defaults
   - Ensure README, `.env.example`, and implementation constants agree

## AI/Copilot usage

Copilot (cloud agent) performed the following work for issue #70:

- Explored repository with semantic search to identify all configuration constants across `orchestration_service.py`, `misty_controller.py`, `.env.example`, `README.md`, and `docs/IMPLEMENTATION_GUIDE.md`.
- Identified six concrete default values that had drifted between documentation and code:
  - `FOUNDRY_API_TIMEOUT`: `.env.example` said `5.0`, code had `10.0`
  - `SERVICE_TIMEOUT`: `.env.example` said `6.0`, code had `15.0`
  - `KOKORO_VOICE`: `IMPLEMENTATION_GUIDE.md` said `af_heart`, code had `af_sky`
  - `MAX_CONTEXT_CHARS`: `IMPLEMENTATION_GUIDE.md` said `3000`, code had `5000`
  - `RECORDING_DURATION_S`: guide said `4` s, code had `6` s
  - `FOLLOWUP_LISTEN_S` / `FOLLOWUP_TIMEOUT_S` / `WATCHDOG_IDLE_TIMEOUT_S`: guide had stale values
- Created `src/windows-orchestration/config_defaults.py` as the canonical source with a maintenance note.
- Updated `orchestration_service.py` and `misty_controller.py` to import defaults from `config_defaults`.
- Fixed `.env.example` to match actual defaults and added previously missing misty_controller variables.
- Updated `docs/IMPLEMENTATION_GUIDE.md` stale values and replaced outdated sensory-reboot description.
- Added `TestCanonicalDefaults` unit test class (18 tests) verifying that both service modules' runtime values match `config_defaults`.
- Ran CodeQL — 0 alerts.
- Ran 192 unit tests — all passed.

## AIC usage snapshot

- **Timestamp**: 2026-06-28T13:24 UTC (session start) through implementation
- **Scope**: current session (approximately 2 hours)
- **Source**: `session_store_sql events.usage_cost`
- **Total AICs**: 442.5
- **Model breakdown**:
  | Model | Input tokens | Output tokens | AICs |
  |-------|-------------|--------------|------|
  | gpt-5.5 | 4,121,098 | 34,125 | 442.5 |

## Repository updates

### Files changed
- `src/windows-orchestration/config_defaults.py` — **new file**: canonical source for all env-configurable defaults
- `src/windows-orchestration/orchestration_service.py` — import `config_defaults`; use its values as `os.getenv` fallbacks for `FOUNDRY_API_TIMEOUT`, `SERVICE_TIMEOUT`, `KOKORO_VOICE`, `KOKORO_SPEED`, `MAX_USER_CHARS`, `MAX_CONTEXT_CHARS`, `TTS_CACHE_MAX`
- `src/windows-orchestration/misty_controller.py` — import `config_defaults`; use its values as `os.getenv` fallbacks for all 14 controller env-configurable constants
- `src/windows-orchestration/.env.example` — fixed `FOUNDRY_API_TIMEOUT` (5.0→10.0), `SERVICE_TIMEOUT` (6.0→15.0), added misty_controller variables, maintenance note pointing to `config_defaults.py`
- `docs/IMPLEMENTATION_GUIDE.md` — corrected 7 stale default values; replaced outdated sensory-reboot description with current re-arm strategy
- `tests/test_integration.py` — added `TestCanonicalDefaults` class (18 tests)

### Issues / PRs
- Implements issue #70 (Centralize shared configuration defaults to prevent drift)
- Branch: `copilot/centralize-shared-config-defaults`

## Logging convention

```
logs\log_YYYY-MM-DD.md
```
