# Log - 2026-06-28

## Requests

1. Implement issue #67: Prewarm greeting and thinking TTS phrases at orchestration startup.

## AI/Copilot usage

- Searched the codebase to locate the existing `_prewarm_tts_cache` function and `_MOVEMENT_ACKS` constant in `orchestration_service.py`, and the `_upload_greeting` method in `misty_controller.py`.
- Added a `_CONTROLLER_PHRASES` constant (`"What's up baby?"`, `"Let me think about that."`) in `orchestration_service.py` immediately after `_MOVEMENT_ACKS`.
- Extended `_prewarm_tts_cache` to append `_CONTROLLER_PHRASES` to the phrase list so the controller phrases are prewarmed and pinned alongside movement acknowledgments.
- Added two new `TestTTSCache` unit tests:
  - `test_controller_phrases_in_prewarm_set`: verifies both controller phrases appear in the list that `_prewarm_tts_cache` builds.
  - `test_controller_phrases_are_pinned_on_cache_put`: verifies that controller phrases stored as `pinned=True` survive cache eviction pressure.
- Verified all 11 `TestTTSCache` tests pass (9 existing + 2 new).
- Ran CodeQL; zero alerts found.
- Committed changes on branch `copilot/prewarm-tts-greeting-phrases`.

## AIC usage snapshot

Snapshot time: 2026-06-28 13:31 +00:00

Scope: day-to-date recorded usage for 2026-06-28.

Source: `session_store_sql` over `events.usage_cost`.

| Model | AICs | Input tokens | Output tokens | Cache read | Cache write |
|---|---:|---:|---:|---:|---:|
| `gpt-5.5` | 6,240 | 74,656,464 | 405,314 | 67,993,088 | 0 |
| `claude-opus-4.8` | 120 | 297,140 | 15,256 | 243,921 | 51,957 |
| **Total** | **6,360** | | | | |

## Repository updates

- Modified `src/windows-orchestration/orchestration_service.py`:
  - Added `_CONTROLLER_PHRASES` constant.
  - Updated `_prewarm_tts_cache` to include controller phrases.
- Modified `tests/test_integration.py`:
  - Added `test_controller_phrases_in_prewarm_set`.
  - Added `test_controller_phrases_are_pinned_on_cache_put`.
- Added `logs/log_2026-06-28.md`.
- Branch: `copilot/prewarm-tts-greeting-phrases` (implements issue #67).

## Logging convention

Create one markdown log per day that repo updates are made, using:

```text
logs\log_YYYY-MM-DD.md
```
